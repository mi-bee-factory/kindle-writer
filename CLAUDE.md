# kindle-writer プロジェクト引き継ぎ書

このファイルは新規セッションが正しい認識で作業を継続できるようにするための引き継ぎ資料です。**作業前に必ず読んでください。**

## プロジェクト概要

- **単一ファイルのWebアプリ**：`index.html`（Kindle出版サポートアプリ）。ビルド不要、Supabase（認証＋DB）を使用
- **公開URL**：`https://mi-bee-factory.github.io/kindle-writer/`
- **GitHubリポジトリ**：`mi-bee-factory/kindle-writer`（`main`ブランチ、GitHub Pages自動デプロイ）
- **ローカルパス**：`C:\Users\81907\Desktop\bioiofactory\kindle-writer\index.html`

## ロール構成（3種類）

| ロール | 呼称 | 権限 |
|---|---|---|
| `owner` | オーナー | 全プロジェクトを無制限に閲覧・共同管理。プロデューサーへの共有許可・ユーザー管理・招待URL発行ができる唯一のロール |
| `admin` | プロデューサー | 自分のプロジェクトを管理。オーナーが「共有」を許可した他人のプロジェクトも管理できる。ライターへの招待URL発行、本のとりまとめができる |
| `member` | ライター | 招待URLで届いた**自分の担当章のみ**編集可能。プロジェクト全体の管理権限は一切なし |

## 重要な設計：本が2冊以上あるときの仕組み（2026-08-12に追加）

### 直した問題
「プロジェクト＝プロデューサーのユーザーID」だったため、同じ人が2冊目の共著を始めると1冊目と同じ枠を指してしまっていた。`member_chapters` の一意制約が `(book_id, user_id, chapter_index)` で `book_id` はアプリ共通の定数だったので、**同じライターが2冊目で同じ章番号を書いた瞬間に1冊目の原稿が上書きされる**状態だった。

### 仕組み
本ごとに `S.book.projectKey` を持たせ、そこから2つのIDを導出する。**キーが無い＝これまでの本＝従来と完全に同じ値**になる（後方互換の要）。

| | これまでの本 | 新しい本 |
|---|---|---|
| `projectKey` | 無し（`'legacy'` 扱い） | `'p'+時刻+乱数` |
| `book_id` | `'kindle-writer-v1'`（不変） | `'kw-'+projectKey` |
| 操作対象ID / `project_id` | ユーザーIDそのまま | `userId#projectKey` |
| `book_settings` | 同じ行の最上位 | 同じ行の `data.archive[key]` |

**`book_id` を分けるのが肝。**`project_id` は一意制約に含まれないので、そこだけ変えても上書きは防げない。`book_id` を分けると既存の制約が自動的に本単位で効くので、**Supabaseのスキーマ変更なしで**成立する。

`book_settings` は1ユーザー1行のまま。アクティブでない本は同じ行の `data.archive` に退避する（**削除しない**）。

### 関連する主要関数
- `_projectKey()` / `_projectBookId()` / `_chapterBookId()`：本の識別子。ライターは招待URLの `bookId` を使う
- `_splitTarget(id)` / `_makeTarget(uid,key)`：`userId#key` の分解・組立
- `_pickProjectData(root,key)`：最上位か `archive[key]` かを解決
- `_buildSwapData(root,updatedAt,key)`：本の入れ替え（純粋関数。通信から切り離してあるので単体で検証できる）
- `_sbActivateOwnProject(key)`：上記を使って1回のupsertで入れ替える。**呼ぶ前に必ず `_sbFlushPendingSaves()`**
- `_sbStartNewProject()`：本物の「＋新しいプロジェクトを始める」
- `_sbProbeBookId()`：新しい `book_id` でライターが保存できるかRLSを事前確認（`chapter_index:9999` のセンチネル行。既存の本には実行されない歯止め付き）

### 絶対に破ってはいけない不変条件
1. **RLSで弾かれても従来の `book_id` にフォールバックしない**（それをやると別の本を上書きする＝防ごうとしている事故そのもの）
2. `book_settings` は必ず read → スプレッド → write。行の削除はしない
3. `_sbSaveProjectData` のガード1：クラウドの `projectKey` と食い違ったら書かずに中断（別タブ対策）
4. 古い招待URL（`bookId` を持たない）は従来どおり `kindle-writer-v1` に落ちる。**配布済みの招待URLを壊さないための保証**

### 既知のリスク
同一アカウントで2タブ開くと、負けた側の書き込みが無効化される（破壊はしないが直前の編集は失われる）。検知時にトーストで再読み込みを促している。

---

## プロジェクトのクラウド保存の仕組み（2026-07-08の改修）

### 過去の問題（修正済み）
以前は「全プロデューサーがオーナー1人のクラウド保存場所に上書き保存」する設計だった。これにより、後から保存した人のデータで前の人のものが消えるリスクがあり、「誰が作ったプロジェクトか分からない」原因にもなっていた。

### 現在の仕組み
- **ユーザー1人につき1つの独立したクラウド保存場所**（`book_settings`テーブル、`id`カラム＝そのユーザーのuser id）を持つ。
- 保存のたびに「誰が最後に保存したか」（`updatedByName`）を記録する。
- **「管理するプロジェクトを選ぶ」パネル**（ホーム画面、オーナー・プロデューサー両方に表示）から、他の人のプロジェクトを選んで「これを管理する」を押すと、**その人と完全に同じデータを共同編集**できる（`S._activeTargetId`で「今どのプロジェクトを操作対象にしているか」を管理）。

### 権限モデル（重要）
- **オーナー**：全プロジェクト（オーナー自身＋全プロデューサー）が無条件に見える・選べる。
- **プロデューサー**：デフォルトは**自分のプロジェクトのみ**。オーナーが個別に「共有を追加」ボタンで許可した相手のプロジェクトのみ追加で見える。**プロデューサー同士が無条件にお互いのプロジェクトを見られることはない**（オーナーの許可が必須）。
- **ライター**：この「管理するプロジェクトを選ぶ」パネル自体が表示されない。対象外。

### 共有の仕組み（2段階、両方必要）
1. **オーナー**がホーム画面のプロジェクト一覧で「＋◯◯さんを追加」ボタンを押す → そのプロジェクトが相手の一覧に「選べる項目」として現れる（まだ自動では切り替わらない）
2. **共有された本人**が、自分の画面で該当プロジェクトの「これを管理する」ボタンを押す → ここで初めて操作対象が切り替わり、共同編集が始まる

保存しただけでは自動的に共有されない。この2段階を経て初めて、両者は同じクラウド保存場所（同じ`book_settings`の行）を見るようになり、以降はどちらが保存しても自動的に反映される（真の双方向共同編集）。

### 関連する主要関数（index.html内）
- `_activeTargetId()`：現在操作対象にしているプロジェクトのuser idを返す（未設定なら自分自身）
- `_sbSaveProjectData()` / `_sbLoadProjectData(targetId)`：クラウドへの保存・読み込み。読み込み時に`updatedByName`等も復元
- `_sbListManagedProjects()`：一覧取得。オーナーは無制限、プロデューサーは自分＋共有許可されたものだけにフィルタ
- `_sbShareProject(targetId, producerId, grant)`：共有の許可／解除（オーナーのみ実行可）
- `_sbSwitchTarget(id)`：「これを管理する」ボタンの処理。指定プロジェクトを読み込んで操作対象に切り替える
- `renderHomeProjectPanel()` / `_renderHomeManagedProjects()`：ホーム画面の「管理するプロジェクトを選ぶ」パネルの描画

### 「章の数」について
`S.people`という変数が「人数」と「章スロット数」を兼ねている（変更していない、既存仕様のまま）。UIの文言は「何人分を管理しますか？」から**「章の数を設定してください」**に変更済み。**1人が複数章を担当する場合は、章タイトル欄の「担当者名」に同じ名前を複数スロットへ入力すればよい**（技術的にバリデーション無しの自由入力なので既に可能）。目標文字数は`totalTarget / 章の数`で算出されるため、章単位で自動的に均等（＝平等）になる。

## 本文の改行・段落の扱い（2026-08-12に追加）

### 直した問題
本文の変換が**書き出し・特別章・プレビュー・まとめの4箇所でバラバラに実装**されていて、しかもどれも**書き手が入れた空行を捨てていた**。「ここでひと呼吸」と思って空けた1行が、本では無かったことになっていた。

### 仕組み
4箇所すべてを **`_bodyLinesToHtml(raw, renderLine, opts)`** に集約した。**プレビューと実EPUBが必ず一致する**のがこの関数の存在理由（過去にQRコードのサイズでプレビューだけ正しく実機が違う、という事故があったため）。

- **書き手の空行** → 直前の段落に `class="pb"`（`p.pb{margin-bottom:1.9em}`）。**空の `<p>` は作らない**（Kindleが空段落を削ることがある）
- 空行が何行続いても余白は1段階まで。見出し・図の直前直後では付けない
- **自動余白（既定オフ）**：空行が一度も無い章に限り、N段落ごとに余白を入れる。ただし **`。！？」』` で終わる段落の直後だけ**。文の途中では絶対に切らない
- **書き手が空行を1つでも使っている章では自動処理は発動しない。人が入れた区切りが常に機械に勝つ**

CSSは `EPUB_PREVIEW_CSS`・実EPUBの `style.css`・`buildBookHTML` の3箇所すべてに入れること。

### 点検と3つの対処
`_paraFlowIssues(body)` が「文の途中の改行」と「空行なしで続く塊」を検出し、`_chapFlowBadge` が章一覧にバッジを出す。対処は3通り用意してある：
1. `_coChapFlowHtml` の「ひと呼吸ほしいと伝える」→ 既存の修正依頼（`reviseNote`）を**下書き**する（自動送信しない）
2. 確認モーダルの編集欄でみほさん自身が空行を入れる
3. まとめ画面の自動余白スイッチ（最終手段）

**アプリが原稿（`c.body`）を勝手に書き換えることはしない。**すべて書き出し時のレイアウト処理。

---

## バックアップと復元（2026-08-12に追加）

### 直した問題
- **`save()` が保存失敗を握りつぶしていた。**ブラウザの保存容量を超えると、以降ずっと何も保存されないのに画面は正常に見える状態だった。表紙を縮小せずに入れていたので、表紙1枚で数MB使うことがあった
- **`buildUploadEpub()` が無警告で表紙・特典・本のID・全章を置き換えていた**（コード上のコメントにも「復元しない」とあった）
- バックアップ機能が一切なかった

### 仕組み
**本ごとに2ファイル**を出す（端末まるごとではない。みほさんの「その都度プロジェクトごとに落とすほうがいい」という判断に従っている）。

| ファイル | 用途 | 戻せるもの |
|---|---|---|
| `.kwbook.json` | そのまま復元する | 画像・表紙・特典・表題紙まで全部 |
| `_原稿.txt` | メモ帳で直す | 文章と章タイトルのみ |

- `_kwBuildBackup(state,meta)` / `_kwParseBackup(text)` は**純粋関数**なので単体で検証できる
- 画像は `kwasset:a1` の参照に置き換え、実体は末尾の `assets` にまとめる。**冒頭が日本語で読める**のがこの形式の理由。同じ画像は1つに集約される
- **`S.apiKey`（平文のAPIキー）は必ず除外する。**`KW_BACKUP_OMIT` を減らさないこと
- `exportBookAsSplitText()` は既存の `manualSplitChapters()` がそのまま読み戻せる形で出す。**説明文に「章区切り」という文字列を書いてはいけない**（その語を含む行が区切りとみなされ、説明文自体が区切りとして誤検出される。実際に踏んだ）

### 復元の不変条件
1. **必ず `pmNewProject(name,{keepState:true})` で新しいスロットに復元する。**既存の `kdpwriter_p*` には一切触らない
2. **`S._activeTargetId=null` にして、復元直後にクラウドへ自動送信しない。**これが無いと、古い控えの復元が新しいクラウドデータを上書きする
3. `S.book.uuid` は既定で新規採番（同じIDの本が2つあるとKDPが改訂版として扱う）

### 壊れる操作の歯止め
`snapshotBook(label)` / `undoLastBookChange()`（メモリ上に最大3件）。`buildUploadEpub`・`manualSplitChapters`・分割リセット・クラウド取り込みに入れてある。`pmDelete` は削除前に `_kwBackupProjectById(id)` を強制実行する。

---

## 本の体裁（2026-08-12に追加）

**`bookCss(sel)` が本文CSSの唯一の出どころ。**`sel=''` で実EPUB、`sel='.epubPrev '` でプレビュー。以前は3か所が別々に同じCSSを持っていて既にずれていた。**直書きに戻さないこと。**

- 設定は `S.book.typo`。`S.book` の下なのでクラウド同期にもバックアップにも自動で乗る
- **文字サイズと書体は項目として出していない。**Kindleは読者側で変えられるので、出すと「プレビューでは合っているのに実機で違う」を再生産する（QRコードのときと同じ事故）
- プレビューには `body` が無いのでページ余白は `.epubPrev{padding}` として出す。**この非対称は `bookCss()` の中に閉じ込めてある。**外に出すと余白だけプレビューに反映されなくなる
- 検証は目視でなく `style.css === bookCss('')` のアサーションで行う（全設定の組み合わせで確認済み）

## クラウド保存の負荷（2026-08-12）
`_sbSaveProjectData` は**内容が変わったときだけ**送る（`_kwHash` で指紋を取る。`archive` は含めない）。デバウンスは15秒。archive に過去の本を入れる設計上、行全体が数MBになるため、無変更の再送を止める必要があった。

---

## 現在の実データ状況（2026-07-08時点）

- **「あなたの好きが未来を創る　STORY2」** = オーナー自身のクラウド保存場所にあるプロジェクト。以前の仕様でプロデューサー（しほりさん）が保存していた内容がここに蓄積されていた。
- **しほりさん（プロデューサー）**：オーナーから既にSTORY2への共有許可は出た（付与済み）。ただし**しほりさん本人がまだ「これを管理する」を押していない可能性がある**（本人に確認・依頼が必要）。彼女が押すまでは、彼女自身のクラウド保存場所は空（「🆕 あなたの新しいプロジェクト」）のまま。
  - 彼女のローカルブラウザ内のデータ（今まで実際に書いてきた原稿）は、コードのこの修正では一切触っていないため消えていない。
  - 彼女が「これを管理する」を押すと、STORY2のクラウド内容で画面が上書きされる。基本的にはSTORY2の内容と彼女のローカル内容は一致しているはずなので実害はないが、**直前に保存していない最新の入力があれば失われる可能性がある**ので、押す前に一呼吸置いて保存を確認するよう伝えるとよい。
- **「テスト管理者」**：オーナー自身が別ブラウザで作成した検証用プロデューサーアカウント。オーナー本人の別人格。

## デプロイ運用上の注意（重要・頻発した問題）

GitHub Pagesへのデプロイが**正常にpushしてもワークフローが発火しない**ことが複数回発生した（原因不明、GitHub側の一時的な問題と推測）。**プッシュ後は必ず以下の手順でデプロイ成功を確認すること：**

```bash
# 1. 最新コミットのSHAを確認
curl -s "https://api.github.com/repos/mi-bee-factory/kindle-writer/actions/runs?per_page=3" | grep -E '"head_sha"|"status"|"conclusion"'

# 2. 該当SHAの実行が見当たらない、または conclusion が failure の場合は空コミットで再トリガー
git commit --allow-empty -m "GitHub Pagesデプロイ失敗のため再デプロイをトリガー"
git push

# 3. 反映確認（該当コミット固有の新しい文字列で検索）
curl -s "https://mi-bee-factory.github.io/kindle-writer/?_=$(date +%s)" | grep -c "確認したい新しい文字列"
```

**本番DBへの直接クエリ（`_SB_URL`/`_SB_KEY`を使ったcurl等）は、ユーザーの明示的な許可なく実行しないこと。**（実際に一度、自動許可判定でブロックされた実績あり。個人情報を含むテーブルの生データダンプになるため。）

## 作業の進め方（このセッションで確立した運用ルール）

1. 複雑な構造変更・データモデルに関わる修正は、必ずPlan Modeで調査→計画→承認を経てから実装する。
2. 実装前に3つのExploreエージェントを並行して立ち上げ、憶測でなくコードを正確に調査してから計画を書く。
3. 実装後は必ず`preview_console_logs`でエラー無しを確認してからプッシュする。
4. プッシュは「プッシュしてください」の一言で、こちらが`git add / commit / push`まで代行する（ユーザーはGitHub操作をしない）。
5. プッシュ後は上記の「デプロイ運用上の注意」の手順で必ず反映確認まで行う。反映確認が取れるまでユーザーに「完了」と報告しない。
6. **EPUBの見た目に関わる修正は、実際に生成されたファイルの中身で確認する。**コードを読んだだけ・アプリ内プレビューだけで「直りました」と言わない。過去にQRコードのサイズで、プレビューは正しいのに実機Kindleでは2.5倍という事故があった。
   ブラウザで検証するときは `zipStore` を一時的に差し替えると、ダウンロードせずに生成されたXHTMLを直接読める：
   ```js
   const orig=window.zipStore;let cap=null;
   window.zipStore=(f)=>{cap=f;return new Blob([]);};
   await buildEpub();
   window.zipStore=orig;
   new TextDecoder().decode(cap.find(f=>f.name==='OEBPS/ch1.xhtml').bytes)
   ```
7. **`_sb` は `const`、`_sbUser` は `let` のモジュールスコープ変数なので、`window._sb=...` で差し替えられない。**Supabaseまわりを検証したいときは、通信から切り離した純粋関数（`_buildSwapData` など）を作ってそれを直接テストする。
